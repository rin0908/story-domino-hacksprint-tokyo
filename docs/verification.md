# 検証記録

2026年9月12日、macOS / Node.js 22.23.0 / npm 10.9.8 で確認。

## 自動テスト

`npm test`：75件通過、失敗0件。テスト内のprovider doubleは実サービスの成功証拠には数えません。

- API方式ごとの接続設定、登録・設定・接続成功の区別。
- Neo4j由来の影響IDを後段へ渡す契約、不正なID集合の拒否。
- Nosanaの短文接続確認が済む前の本生成拒否。
- 生成・検査失敗時に成功を返さず、検査前／不合格時に採用不可。
- 採用・取り消しによる5場面／前提／オチの復元。
- 複数runの分離、処理中の二重操作、期限切れ。
- 外部例外に含まれる認証情報を表示しない固定エラー。
- Daytonaへ送るものと同じ固定Python検査を**ローカルPythonで**実行。ID欠落・重複・文字列／真偽値ID・維持場面の改変・空本文を拒否。
- Python風の本文や引用符もデータとして読み取り、コードとして実行しない。

## ブラウザーで確認したこと

専用のChrome検証セッションで実際のローカルサーバーを開いて確認。

- 初期表示：5場面、作品名、実行／サンプル／接続確認ボタンが表示される。
- サンプルを選択：場面3・4・5をサンプル対象として表示、前後比較と事前作成の開示を表示。
- サンプル採用：5場面を保持し、場面1・2の本文は完全一致。場面3〜5とオチがサンプルへ変わる。
- 元に戻す：5場面・前提・元のオチへ復元する。
- サンプルから実実行へ切り替え：Neo4j未設定を失敗として表示。実検索の強調0件、サンプルバナーなし、採用不可。
- 接続確認：不足設定を表示し、0/3接続成功と正しく表示する。
- 390pxのスマートフォン幅：ページ幅390px、横方向のはみ出しなし、5場面が存在。
- 実サービスを使うブラウザー実行で、モデルが場面5を空本文にした回はDaytonaが不合格と判定し、採用ボタンが無効になった。本文の自動補完やサンプルへの置き換えはしていない。
- 最終のOllama JSON Schema設定で実サービス通し操作が成功。変更ボタンからNeo4jの3・4・5検索、Nosana `qwen3.5:9b` の実生成、Daytonaの7項目合格を表示。変更ボタンを押して16秒後の確認時点で完了していた。正確な処理時間の計測値ではなく、確認時点の上限。
- 実生成の内容は「古いタブに気づく→全員が誰かが切り替えると思っていた→正しいタブに切り替えて表示」という展開。これは当該実行のモデル応答であり、毎回同じ内容の保証ではない。
- 実生成を採用後、場面1・2のタイトルと本文が原作に完全一致し、3・4・5が変わったことをDOM比較で確認。元に戻す操作で5場面のタイトル・本文、前提、オチがすべて元どおりになった。
- JavaScript実行エラーとフレームワークのエラー画面は検出されなかった。

自動操作ツールが画面外のボタンを押せない場面があったため、対象を表示領域へスクロールして再確認した。実際の採用・取り消しの表示更新を確認済み。

## 実サービスで確認したこと

| 項目 | 結果 |
|---|---|
| 3サービスのアカウント・スポンサークレジット | 本人申告で取得済み |
| Neo4j接続と実グラフ検索 | 確認済み。場面3・4・5の3経路を取得 |
| Nosana短文応答と物語生成 | 確認済み。Ollama `qwen3.5:9b` の短文応答と5場面のJSON生成に成功 |
| Daytona Sandboxの実Python検査 | 実生成結果の7項目すべて合格。別途、明示したサンプルで正常データ合格、場面1改変・ID欠落重複の不合格も確認 |
| 実検索→実生成→実隔離検査→採用→取り消し | サーバーの実サービス通し検証で成功。採用後も場面1・2が一致し、取り消しで5場面・前提・オチが復元 |
| ブラウザーによる実サービス通し操作 | 確認済み。実検索・実生成・実検査・採用・取り消しが成功。DOM比較で場面1・2維持と全体復元を確認 |
| 説明を含む2分の発表リハーサル | 未計測。2分の操作台本を用意。処理完了はボタン操作から16秒後の確認時点で観察 |

2026年9月12日15:03:51（日本時間）開始の `npm run verify:live` で、実サービスのみを使用して成功。接続確認は3件とも成功し、実検索は影響ID `[3,4,5]` と維持ID `[1,2]`、実生成は `qwen3.5:9b`、実検査は終了0・7項目合格、`adoptUndoVerified: true` を記録しました。これは形式と復元処理の証拠であり、生成文の意味が正しいという自動判定ではありません。

NosanaはGPU割り当て後の初回準備中に45秒でタイムアウトし、モデル準備完了後の再試行で成功しました。モデルの初回準備時間と当日の実演所要時間は別々に扱います。

## 提出資料

PDFドラフト：5ページ、16:9、130,145 bytes（10MB未満）。3サービスのブラウザー通し操作成功と、未計測の2分発表リハーサルを区別しています。最新PDFの全5ページをPNG表示して目視確認し、文字切れ・重なり・文字化けなし。文字抽出も確認済み。

本人の承認後、[公開GitHubリポジトリ](https://github.com/rin0908/story-domino-hacksprint-tokyo)へ30ファイルを公開しました。初回公開時にGitHubから取得した全30ファイルのSHAが、ローカルのGit blob SHA-1と一致しました。本人承認後の2026-09-12 15:46 JSTに提出フォームを送信し、URL末尾が `formResponse` の画面で「回答を記録しました。」を確認しました。参加者の氏名・メールは公開しません。提出PDFは130,145バイトのファイルをそのまま保持し、提出後は変更していません。

`npm run verify:live` は実サービスのみで接続・検索・生成・検査・採用/取り消しを試すスモーク検証。`npm run verify:daytona` は明示したサンプルとその異常データを実Sandboxで検査します。証拠はGit除外の `.runtime/` へ保存します。実ブラウザー操作の結果は `browser-evidence.json`（2026年9月12日15:13:53、日本時間）に、サーバー処理は `live-evidence.json` に記録しました。`verify:daytona` のサンプル検証はAI生成の成功証拠には使いません。
